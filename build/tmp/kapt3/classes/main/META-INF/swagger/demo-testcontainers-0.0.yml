openapi: 3.0.1
info:
  title: demo-testcontainers
  version: "0.0"
paths:
  /customers:
    get:
      operationId: getPersons
      parameters: []
      responses:
        "200":
          description: getPersons 200 response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Object'
components:
  schemas:
    Object:
      type: object
